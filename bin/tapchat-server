#!/usr/bin/env coffee

path = require 'path'
fs   = require 'fs'

lib = path.join(path.dirname(fs.realpathSync(__filename)), '..', 'lib')

Engine = require path.join(lib, 'tapchat-server', 'engine')

PORT         = 8080
BACKLOG_FILE = '/tmp/backlog.db'

@engine = new Engine BACKLOG_FILE, PORT
