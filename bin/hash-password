#!/usr/bin/env coffee

PasswordHash = require('password-hash')

argv = process.argv

password = process.argv[2]

unless password
  console.log 'Usage: hash-password your_password'
  return

console.log PasswordHash.generate(password)
